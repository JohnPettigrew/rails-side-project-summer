<h3>Take part in #SideProjectSummer!</h3>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label 'Your name' %>
    <%= f.text_field :name, autofocus: true, autocomplete: "name", :tabindex => 1 %>
  </div>

  <div class="field">
    <%= f.label 'Your email address' %><%= link_to ' (Check our privacy policy)', privacy_path %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", :tabindex => 2 %>
  </div>

  <%= f.fields_for :projects, Project.new do |p|  %>
    <div>
      <%= p.label 'Name the project you will start with' %>
      <%= p.text_field :name, :tabindex => 3 %>
    </div>
    <div>
      <%= p.label 'Briefly describe your project' %>
      <%= p.text_area :description, :rows => 3, :tabindex => 4 %>
    </div>
  <div>
    <%= p.label :source, "Where will your source code be stored (e.g. GitHub or Bitbucket)?" %>
    <%= p.url_field :source, class:'form-source', :tabindex => 5 %>
    <p class="small">Please include a complete URL to the repository, or leave this field blank. You can update this later if you need to.</p>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %>
    <%= f.password_field :password, autocomplete: "new-password", :tabindex => 6 %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %>
    <%= f.password_field :password_confirmation, autocomplete: "new-password", :tabindex => 7 %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up", :tabindex => 8 %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
